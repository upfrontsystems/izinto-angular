import { TouchListenerDirective } from './touch.listener.directive';

describe('TouchListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchListenerDirective();
    expect(directive).toBeTruthy();
  });
});
