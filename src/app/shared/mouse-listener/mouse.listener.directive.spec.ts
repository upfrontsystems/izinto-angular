import { MouseListenerDirective } from './mouse.listener.directive';

describe('MouseListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseListenerDirective();
    expect(directive).toBeTruthy();
  });
});
